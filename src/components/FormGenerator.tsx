import * as React from "react";

interface IProps {
  schema: {};
  formData: {};
  onChange: () => any;
}

const FormGenerator: React.FC<IProps> = () => <form></form>;

export default FormGenerator;
